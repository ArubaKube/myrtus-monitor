[project]
name = "myrtus-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["argparse>=1.4.0", "pydantic>=2.11.5"]

[dependency-groups]
dev = ["ruff>=0.11.12"]

[tool.uv]
package = true

[project.scripts]
node-monitor = "monitor.node.main:main"
